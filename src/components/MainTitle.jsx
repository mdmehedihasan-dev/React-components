
// eslint-disable-next-line react/prop-types
const MainTitle = ({miniStyle,minititle}) => {
  return (
    <div>
      <p className={miniStyle} >{minititle}</p>

    </div>
  )
}

export default MainTitle