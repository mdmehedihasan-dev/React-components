

// eslint-disable-next-line react/prop-types
const HeadTitle = ({headtitle,headStyle}) => {
  return (
     
    <div>
     
        <h1 className={headStyle} >{headtitle}</h1>
    </div>
  )
}

export default HeadTitle